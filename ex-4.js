// Start coding here
const calculator = {
    add: add,
    subtract: subtract,
    multiply: multiply,
    divide: divide
  };
  
  // Execute add function with a = 10, b = 20
  const addResult = calculator.add(10, 20);
  console.log(addResult); // 30
  
  // Execute divide function with a = 3000, b = 10
  const divideResult = calculator.divide(3000, 10);
  console.log(divideResult);